#include <stdio.h>
#include <string.h>
#include "find.h"



int main() {
    find(sports_or_workout);
    find(sports_no_bieber);
    find(ns_theater);
    find(arts_theater_or_dinig);
    return 0;
}